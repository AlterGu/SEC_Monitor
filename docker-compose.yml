version: "3.8"

services:
  sec-monitor:
    build: .
    container_name: sec-monitor-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - sec-data:/app/data
    environment:
      - DB_PATH=/app/data/sec_monitor.db

volumes:
  sec-data:
